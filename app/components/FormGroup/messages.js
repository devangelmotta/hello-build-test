/*
 * FormComponents Messages
 *
 * This contains all the text for the FormComponents component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'hellobuild.components.FormComponents';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormComponents component!',
  },
});
