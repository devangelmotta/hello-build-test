/*
 * GoogleCalendar Messages
 *
 * This contains all the text for the GoogleCalendar container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GoogleCalendar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleCalendar container!',
  },
});
