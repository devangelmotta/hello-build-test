// import { selectGoogleCalendarDomain } from '../selectors';

describe('selectGoogleCalendarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
